
{
  "name": "idea-clay-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",

    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "openai": "^4.2.1",
    "dotenv": "^16.0.3"
  },
  "engines": {
    "node": "18.x"
  }
}